# Store Management System

## Current State
Project scaffolding exists (frontend + backend folders) but no application code has been written yet.

## Requested Changes (Diff)

### Add
- Product catalog management (add, edit, delete products with name, SKU, category, price, stock quantity)
- Inventory tracking (stock levels, low-stock alerts)
- Category management (create and assign categories)
- Sales/orders management (record sales, update stock automatically)
- Dashboard with key metrics (total products, low stock items, total sales value)
- Simple search and filter for products

### Modify
- N/A (new project)

### Remove
- N/A (new project)

## Implementation Plan
1. Generate Motoko backend with Product, Category, and Order data models and CRUD operations
2. Build frontend with Dashboard, Products, Categories, and Orders pages
3. Implement navigation between sections
4. Wire all backend calls to frontend
